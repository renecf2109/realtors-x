import Link from "next/link";
import type { ListingImportSummary } from "@/lib/types";

export function ImportHistory({ imports, admin = false }: { imports: ListingImportSummary[]; admin?: boolean }) {
  return <section><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">{admin ? "All workbench uploads" : "Your workbench uploads"}</p><h1 className="mt-2 text-4xl font-black">Listing imports</h1></div>{!admin ? <Link href="/dashboard/imports/new" className="btn">New import</Link> : null}</div>{imports.length ? <div className="mt-8 space-y-4">{imports.map(item => <article key={item.id} className="card p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><h2 className="font-black">{item.file_name}</h2><p className="mt-1 text-xs text-ink/45">{new Date(item.created_at).toLocaleString()} · {item.total_rows} rows</p></div><span className="rounded-full bg-lime px-3 py-1 text-xs font-bold capitalize text-sage">{item.status.replaceAll("_"," ")}</span></div><div className="mt-5 grid grid-cols-2 gap-3 sm:grid-cols-5"><Count label="Imported" value={item.imported_rows} color="text-emerald-700"/><Count label="Optional skipped" value={item.optional_skipped_rows} color="text-blue-700"/><Count label="Review" value={item.review_rows} color="text-amber-700"/><Count label="Missing" value={item.failed_rows} color="text-red-700"/><Count label="Duplicate" value={item.duplicate_rows} color="text-slate-600"/></div></article>)}</div> : <div className="card mt-8 p-12 text-center"><h2 className="text-xl font-black">No imports yet</h2><p className="mt-2 text-sm text-ink/50">Upload a CSV or Excel file to create the first organized import.</p>{!admin ? <Link href="/dashboard/imports/new" className="btn mt-6">Start an import</Link> : null}</div>}</section>;
}
function Count({ label, value, color }: { label: string; value: number; color: string }) { return <div className="rounded-2xl bg-cream p-3"><p className="text-xs text-ink/45">{label}</p><p className={`mt-1 text-xl font-black ${color}`}>{value}</p></div>; }
